name: Corrected start middle late compression benchmark

on:
  push:
    paths:
      - "bundle/cmix-compression-full-dict-start-mid-late.tar.gz.enc"
      - ".github/workflows/compression-full-dict-start-mid-late.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cmix-compression-full-dict-start-mid-late
  cancel-in-progress: false

jobs:
  benchmark:
    runs-on: ubuntu-22.04
    timeout-minutes: 350
    steps:
      - name: Check out encrypted benchmark bundle
        uses: actions/checkout@v5

      - name: Decrypt and verify private bundle
        env:
          CMIX_BUNDLE_KEY: ${{ secrets.CMIX_BUNDLE_KEY }}
        run: |
          set -euo pipefail
          test -n "$CMIX_BUNDLE_KEY"
          mkdir -p private
          openssl enc -d -aes-256-cbc -pbkdf2 -iter 200000 \
            -in bundle/cmix-compression-full-dict-start-mid-late.tar.gz.enc \
            -out /tmp/cmix-compression-full-dict-start-mid-late.tar.gz \
            -pass env:CMIX_BUNDLE_KEY
          tar -xzf /tmp/cmix-compression-full-dict-start-mid-late.tar.gz -C private
          rm -f /tmp/cmix-compression-full-dict-start-mid-late.tar.gz
          (cd private && sha256sum -c SOURCE_SHA256SUMS >/dev/null)
          chmod 700 private/run_benchmark.sh
          echo "Encrypted source bundle verified."

      - name: Build, prepare streams, and test start middle late regions
        id: benchmark
        continue-on-error: true
        run: |
          set -euo pipefail
          cd private
          ./run_benchmark.sh

      - name: Show non-sensitive metrics
        if: always()
        run: |
          for file in environment.txt build_sha256.txt preprocess.tsv sample_meta.tsv pretrain.tsv core_results.tsv; do
            if test -f "private/output/$file"; then
              echo "=== $file ==="
              cat "private/output/$file"
            fi
          done
      - name: Upload metrics only
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cmix-compression-full-dict-start-mid-late-metrics
          path: |
            private/output/environment.txt
            private/output/build_sha256.txt
            private/output/preprocess.tsv
            private/output/sample_meta.tsv
            private/output/pretrain.tsv
            private/output/core_results.tsv
          if-no-files-found: warn
          retention-days: 14

      - name: Encrypt private diagnostics
        if: always()
        env:
          CMIX_BUNDLE_KEY: ${{ secrets.CMIX_BUNDLE_KEY }}
        run: |
          set -euo pipefail
          if test -d private/output/logs; then
            tar -C private -czf /tmp/cmix-compression-full-dict-private-diagnostics.tar.gz \
              output/logs output/environment.txt output/build_sha256.txt \
              output/preprocess.tsv output/sample_meta.tsv output/pretrain.tsv output/core_results.tsv \
              2>/dev/null || true
            if test -s /tmp/cmix-compression-full-dict-private-diagnostics.tar.gz; then
              openssl enc -aes-256-cbc -pbkdf2 -iter 200000 -salt \
                -in /tmp/cmix-compression-full-dict-private-diagnostics.tar.gz \
                -out cmix-compression-full-dict-private-diagnostics.tar.gz.enc \
                -pass env:CMIX_BUNDLE_KEY
            fi
          fi

      - name: Upload encrypted diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cmix-compression-full-dict-private-diagnostics-encrypted
          path: cmix-compression-full-dict-private-diagnostics.tar.gz.enc
          if-no-files-found: ignore
          retention-days: 7

      - name: Remove decrypted material
        if: always()
        run: |
          rm -rf private
          rm -f /tmp/cmix-compression-full-dict-start-mid-late.tar.gz
          rm -f /tmp/cmix-compression-full-dict-private-diagnostics.tar.gz
          rm -f cmix-compression-full-dict-private-diagnostics.tar.gz.enc

      - name: Enforce benchmark success
        if: always()
        run: test "${{ steps.benchmark.outcome }}" = "success"
